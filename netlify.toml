[build]
  command = "pnpm build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["sharp"]

[[headers]]
  for = "/bespoke-palette.json"
  [headers.values]
    Cache-Control = "no-cache, must-revalidate"
